function [] = run_per_unit_spike_detection_per_unit_on_cluster()
% add the path
home_dir = cd("..");
cd("..");
addpath(genpath(pwd));
cd(home_dir);

% get the config
config = spikesort_config();

% get run the filter for the data


end